services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - app_data:/app/data
      - app_db:/app/db
    environment:
      - CHAT_DB_PATH=/app/db/chat_memory.db
    restart: unless-stopped

volumes:
  app_data:
  app_db:
